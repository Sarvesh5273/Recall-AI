# Recall AI — Test Configuration
# pytest discovers test_*.py files in backend/ directory
# Run: cd backend && pip install pytest && pytest -v

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
